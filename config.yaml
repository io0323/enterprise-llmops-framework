# ELF 設定。閾値・上限はすべてここから読む(CLAUDE.md 絶対ルール12)。
system: elf                       # SDK利用時は LLMOps.load(system=...) が優先される

paths:
  db: "data/llmops.sqlite3"
  prompts_dir: "prompts"
  models_file: "models.yaml"
  evals_dir: "evals"
  report_dir: "output"
  policies_file: "policies.yaml"

gateway:
  retry_max: 2                    # 初回+再試行。既存実装(max_retry:1)と等価
  default_model: chat-standard    # front matter にも呼び出しにも model が無いときの既定(論理名)
  timeout_sec: 600
  allow_unpublished: false        # true は開発時のみ。本番 true なら起動時に警告
  allow_raw_completion: false     # Registry管理外の呼び出し。移行期間のみ true
                                  # DDE / CGMP の Prompt 外部化が終わったので false へ戻した
  fallback_enabled: true

guard:
  calls_per_trace_limit: 10       # CGMP 絶対ルール3 を ELF 側で強制
  calls_per_trace_limit_by_system: # system 別の上書き。0 で無制限
    # DDE は「1ラン = 1 trace」で、1ランに expand / classify / cluster_naming が
    # 何十回も入る。記事単位の上限を持たないシステムなので上限を外す(NOTES.md N-023)
    dde: 0
    # ELF 自身(評価実行)。1 run = 1 trace で、ケース数 × (生成 + Judge) ぶん呼ぶ
    elf: 0
    # Harness。1パイプライン実行 = 1 trace で、候補数 × 最大3回再生成 × (生成 + 評価) を呼ぶ。
    # 候補数は Harness の strategy.yaml が持つので ELF 側で二重に数えない。
    # 有料APIの歯止めは回数ではなく金額: budgets の system=harness(NOTES.md N-044)
    harness: 0
  hard_quota: true

budget:
  # **実課金の上限**(USD/月)。判定の対象は models.yaml で billable: true の
  # 論理モデルだけ(現状は anthropic_sdk 系 = Harness)。`claude -p` 経由の
  # コストはサブスク利用の換算値で、追加課金が無いため対象外(NOTES.md N-045)。
  # サブスク換算ぶんも記録・レポートには載る(`llmops report cost`)
  global_monthly_usd: 20.0
  warn_percents: [50, 80, 100]
  # scope 別上限は `llmops budget set` で budgets テーブルへ登録する

trace:
  record_request_text: true       # 入出力全文記録(CGMP 絶対ルール8 を継承)
  record_response_text: true
  max_text_chars: 200000          # 超過分は末尾を切り truncated フラグを立てる
  strict: false                   # true で記録失敗を送出(テスト用。ELF_STRICT_TRACE でも可)

eval:
  judge_model: judge
  regression_tolerance: 0.02      # ベースライン比でこれを超える低下を regressed とする
  deterministic_first: true       # 決定的評価が落ちたら Judge を呼ばない
  min_score: 0.75                 # スイート側で上書き可。ベースラインが無い初版の判定に使う
  # 評価系の安全装置(NOTES.md N-026 の再発防止)。いずれも既定で有効にする
  forbid_judge_fallback: true     # judge 論理モデルに fallback_to があれば実行を拒否する
  fail_on_degraded: true          # degraded=1 の span が混ざった run は無条件で fail
  mock_is_wiring_check: true      # mock を通った run は baseline にしない(配線確認扱い)

# --- 保持期限(Phase 3)-----------------------------------------------------
# 消すのは本文だけ。コスト・トークン・render_hash・Prompt版は残す
# (消すと長期の傾向分析ができなくなる)
anomaly:
  # 使われ方の異常検知(NOTES.md N-046)。**警告するだけで、止めない。**
  # サブスク利用(billable: false)のコストに金額のハード上限は置かない。
  # 置くと「課金していない処理を金銭的理由なく止める」ことになり、N-045 で直した
  # 逆転を作り直す。代わりに「直近の中央値に対する倍率」で普段との差を見る。
  baseline_days: 7                # 中央値を取る日数。0 で検知を止める
  cost_multiple: 3.0              # 中央値の何倍で異常とみなすか
  min_cost_usd: 5.0               # これ未満の日額は倍率を見ない(USD/日)
  trace_multiple: 3.0             # trace 生成数も同じ基準で見る。コストより早く出る
  min_traces: 20                  # これ未満の日次 trace 数は無視する
  # 下限(min_*)は誤検知よけ。静かな日の 0.01 → 1.00 USD を「100倍」と言わないため。
  # **鳴りっぱなしで無視されるレポートは、鳴らないレポートより害が大きい**

retention:
  span_text_days: 180             # これを過ぎた span の入出力本文を消す
  span_days: 730                  # span 行そのものを消す
  archive_dir: "data/archive"     # 消す前に JSONL で書き出す先

logging:
  level: INFO
  file: "logs/llmops.log"
